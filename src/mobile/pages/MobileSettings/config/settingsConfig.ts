import { SettingsConfig } from "../types";

/**
 * Settings configuration
 */
export const settingsConfig: SettingsConfig = {
  appVersion: "1.0.0 AP",
  supportEmail: "support@octopusfinance.com",
  features: {
    biometricAuth: true,
    dataExport: true,
    demoMode: true,
    aiInsights: true,
  },
};
